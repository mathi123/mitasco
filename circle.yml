machine:
  node:
    version: 6.2.1
  post:
    - npm install -g db-migrate@v0.10.0-beta.12 db-migrate-pg typescript@next typings gulp
    - typings install
    - gulp build --env test
    - psql -c 'create database mitasco_test;' -U postgres
    - db-migrate -e test up